<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- compare_remote/ is a separate reference project; exclude it from this project's compile glob -->
  <ItemGroup>
    <Compile Remove="compare_remote/**" />
  </ItemGroup>

</Project>
